defaults:
  - _self_
  - algo: ppo
  
# --- 평가 환경 설정 ---
board_size: 15
num_eval_envs: 100  # 100판 동시 진행
device: cuda        # 평가는 속도를 위해 CUDA 권장 (로컬 테스트 시엔 CLI에서 cpu로 덮어쓰기)

# --- 모델 경로 (기본값) ---
black_checkpoint: "results/evaluation/baselines/0.pt"
white_checkpoint: "results/evaluation/baselines/1.pt"
